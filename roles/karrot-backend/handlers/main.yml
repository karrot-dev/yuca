---

- name: reload systemd daemon
  systemd:
    daemon_reload: yes

- name: restart daphne
  listen: restart app
  service:
    name: "{{ site }}-daphne.service"
    state: restarted

- name: restart workers
  listen: restart app
  service:
    name: "{{ site }}-worker.target"
    state: restarted
